services:
  proxy-check:
    image: ghcr.io/hogskinkitty/proxy-check:latest
    ports:
      - "8080:8080"
    environment:
      - API_KEY=${API_KEY:-}
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
